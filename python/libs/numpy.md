# Numpy

Biblioteca muito usada para o tratamento de dados, cálculos, arrays e matrizes. Primeira coisa a ser feita é criar o ambiente virtual, entrar nele, logo depois instalar e colocar no requirements.txt

```python
python -m venv venv

venv\Scripts\activate
source venv/bin/activate
```

```python
pip install numpy

pip freeze > requirements.txt
```

